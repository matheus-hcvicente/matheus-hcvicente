#include <stdio.h>
 
int main(){
 
    int A;
    float B;
 
    //Armazenamento de valores
    for(A = 0; A < 100; A++){
        scanf("%f",&B);
 
 
        //Condição para mostrar o valor.
        if(B <= 10)
            printf("A[%d] = %.1f\n",A, B);
    }
 
  
    return 0;
}
